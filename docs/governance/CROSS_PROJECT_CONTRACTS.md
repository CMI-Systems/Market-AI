# Market-AI / AICC Cross-Project Contracts

Market-AI may write or read database state only through approved backend/service
interfaces. AICC may consume only authenticated application APIs. AI-DATASET
outputs require supported contract/schema versions, trusted validation status,
provenance, freshness, and explicit environment identity.
